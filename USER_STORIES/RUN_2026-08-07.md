# Rental City automation run — 2026-08-07

5 open code_task tickets found. Two (`ffc0465c`, `8771d7af`) are the same feature
(admin Properties view) described at two different levels of completeness — merged into one
work item, built once, and closed together. 4 real work items total.

## WI-1 — AdminLayout redesign (Pulse: c95e8f2e-ae27-48cb-8be8-c2717cb10197)
Admin shell (`AdminLayout.tsx`) uses a different visual language than the polished
landlord/tenant shell (`TenantLayout.tsx`) — plain SVG icons, non-sticky header, different
sidebar/footer styling.

- AC-01: Given an admin views any admin page, then the header is sticky, `h-16`, with the
  Rental City wordmark and an "Admin" badge.
- AC-02: Given an admin views the sidebar, then nav items use Lucide icons (not inline SVG)
  with the same active-state color tokens (`bg-[#EEF4FE] text-[#3A7AFE]`) as TenantLayout.
- AC-03: Given a non-admin or signed-out user hits any `/admin/*` route, then the existing
  redirect guard still fires unchanged.
- AC-04: No TypeScript errors; footer matches TenantLayout's link pattern.

**PM review:** Ticket didn't explicitly ask for a "Properties" nav entry, but the sibling
Properties ticket (WI-2) requires one and references this ticket's icon convention — added it
here since AdminLayout is the file both tickets touch. **PM APPROVED.**

## WI-2 — Admin Properties view (Pulse: ffc0465c-5212-4ac1-9abb-b0ebce84445f, 8771d7af-9fe2-4550-842a-76e720d45a09 — merged)
Richard (admin) has no way to see landlord-uploaded properties. `ffc0465c` asked for a simple
read-only table (no search/filter, deferred as fast-follow); `8771d7af` asked for the fuller
Zillow-style card grid with filters/sort/search + detail view. Building the simple table first
and replacing it minutes later with the fuller version would be pure waste — built `8771d7af`'s
spec once, which fully satisfies `ffc0465c`'s AC set as a strict subset.

- AC-01: Admin can navigate to `/admin/properties` via a sidebar "Properties" entry.
- AC-02: A card grid loads all properties (any status) with photo, address, rent, beds/baths,
  status badge, landlord name — real Supabase data, joined to landlord via the existing admin
  directory endpoint (no bypassing RLS — `is_admin()` already grants full SELECT).
- AC-03: Filters (status, bedrooms, rent range), search (address), and sort (newest/oldest/rent)
  all work against the fetched set.
- AC-04: Clicking a card opens `/admin/properties/:id` — a read-only detail view (photos,
  description, amenities, landlord link) with NO Apply/Message CTAs — a separate page from the
  consumer `PropertyDetailsPage.tsx`, not a modification of it (that page hardcodes
  `status='active'` and has application CTAs that must never appear in an admin read-only view).
- AC-05: No TypeScript errors; no schema changes required.

**PM review:** Confirmed both tickets describe the same feature at different fidelity —
approved building the superset once rather than twice. **PM APPROVED.**

## WI-3 — Admin dashboard KPI cards + charts (Pulse: a24b4370-668c-4bf3-b4c2-f4df00ebf4ae)
Current dashboard shows 6 plain number cards, no trend/visual data. Client wants 4 meaningful
KPIs with charts and drill-down.

- AC-01: 4 KPI cards — Active Listings, Total Landlords, Open Support Requests, New Sign-ups
  (30d) — each backed by a real Supabase count query, each with a working drill-down link.
- AC-02: Drill-down links actually pre-filter the target page (`?status=active`,
  `?role=landlord`) — not just navigate to an unfiltered list.
- AC-03: A 30-day sign-up trend chart (landlord vs tenant) and a properties-by-status donut,
  both rendered from real fetched data, brand colors only.
- AC-04: `recharts` may be installed if not present (explicitly authorized by ticket text).
- AC-05: No TypeScript errors; clean production build.

**PM review:** Ticket's AC-02 drill-down requirement implies the target pages must support the
query params — flagging that `AdminUsersPage`/`AdminPropertiesPage` had no such support before
this run; added minimal filter UI + param-read wiring to both as part of this ticket rather than
shipping a drill-down link that silently does nothing. **PM APPROVED.**

## WI-4 — Landlord phone number in Admin view (Pulse: 0113bfbc-a6a6-4700-ac98-b0f39ed760ee)
Landlord sign-up only collects email; ticket asks to capture phone at sign-up and show it in
Admin.

- AC-01: Given the real schema, `profiles.phone` already exists (confirmed via psql) — no
  migration needed, contrary to the ticket's Step 2b assumption.
- AC-02: A phone field is added to the landlord onboarding flow (`ProfileCreationPage.tsx`,
  the actual step-2 "set up your profile" screen — not the auth `SignupPage.tsx`, which only
  collects email/password) and persists to `profiles.phone` on submit.
- AC-03: Admin's landlord profile view (`AdminUserDetailPage.tsx`) displays Phone next to Email,
  read-only, showing "Not provided" when null.
- AC-04: Existing landlords (signed up before this change) show blank until they update their
  profile — expected, matches the ticket's own note.

**PM review:** Ticket's Step 2 assumed a migration might be needed — flagging that the schema
check found it already exists, so scope narrowed to wiring only. **PM APPROVED.**
