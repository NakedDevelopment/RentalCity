# Pulse automation run — 2026-07-15

6 open `code_task` items found (not_started), all processed (well under the 20-task cap).

## Task 85204293 — Update lead magnet copy to reflect 'view report' flow instead of 'email report'

Already implemented in commit `1a75c63` (same-day, by the client's own Replit Agent) before
this run started. Verified via `git log`/`git show` against `client/public/rental-value-report/index.html`
— all "on its way to your inbox" / "Email me the full report again" copy already replaced.
Marked done in Pulse, no code changes made.

## Task 18dd543d — Add 'Rental Value' button in left column linking to lead magnet tool

- AC-01: Given a landlord viewing any page under the app shell, then a "Rental Value" nav
  item appears in the left sidebar, opening `https://value.gorentalcity.com/` in a new tab.
- AC-02: Given a tenant, then the item does NOT appear (landlord-only nav array).
- AC-03: Given the sidebar's existing internal nav items, then the new item's active/hover
  styling matches them exactly (no ad hoc classes).

URL resolved by investigation, not assumption: `value.gorentalcity.com` is already the
canonical, consistently-referenced host for this tool across `server/index.ts`,
`server/report-template.ts`, and OG/share metadata — no client input needed.

## Task e1a64c7c / Task 44dd1914 — Lead magnet listing redirect / non-functional bottom link

Investigated as one underlying defect (see BUILD_LOG for reasoning): all "List your
property" CTAs across the lead magnet pointed at the bare marketing root instead of the
signup/listing workflow.

- AC-01: Given a visitor on the rental value report result screen, when they click "List
  your property on Rental City", then they land on the landlord signup form (which itself
  already routes a new landlord into the property-listing wizard on completion) — not the
  marketing homepage.
- AC-02: Given the same CTA in the emailed report and the full shareable report page (both
  header and footer placements), then all three also point to `/signup`.
- AC-03: Given an already-authenticated landlord clicks the CTA, then `PostLoginRedirect`
  (existing app logic) sends them into the app rather than showing them a signup form.

## Task 94310364 — Add VITE_TENANT_SIDE_ENABLED feature flag to hide tenant side for launch

- AC-01: Given `VITE_TENANT_SIDE_ENABLED` unset/false, when any tenant-role user or
  anonymous visitor navigates the app, then no tenant-facing route, nav item, or link is
  reachable — enforced at a single gate (`TenantLayout`) plus two direct route gates
  (`applications/apply`, `/invite/:token`) in `App.tsx`.
- AC-02: Given the flag is false, when checking the browser console/network tab for a
  gated tenant-role user, then no errors or failed calls occur — the gate renders
  `TenantSideComingSoon` before any tenant-only data-fetch effects mount.
- AC-03: Given `vite dev` (local dev server), then the flag is enabled automatically
  (`featureFlags.ts`), so tenant functionality keeps working for continued testing.
- AC-04: Given a landlord-only fix published with the flag off, then publish succeeds
  normally — no separate branch/deploy path, same as before.
- AC-05: Single source of truth — `TENANT_SIDE_ENABLED` from `client/src/lib/featureFlags.ts`
  is the only flag checked anywhere in the diff.

Investigation note: `RoleSelectionPage` already offers landlord-only signup and `SignupPage`
is already hardcoded to `role: 'landlord'` (pre-existing, unrelated to this task) — so no
*new* tenant accounts can be created regardless of this flag. However there are **15 real
tenant-role rows** already in production `profiles` (verified via `psql`), which is why the
flag needed to gate the full shared-page experience (`TenantLayout`), not just onboarding —
those existing users will see a clean coming-soon screen rather than a broken app once the
flag ships with its production default (false).

## Task 608da84e — Move 'Unlock Profile' & 'Approve/Decline' buttons to full match page

- AC-01: Given a landlord on the matches list, then card view no longer shows inline
  Unlock/Approve/Decline/Undo-decline buttons — a single "Review to unlock profile" /
  "Review to decide" link takes them to the match detail page instead.
- AC-02: Given the landlord is on the detail page (reached via the list, now carrying
  `?application=<id>`), then Unlock, Approve, Decline, and (new) Undo-decline all work,
  correctly wired to the same Stripe $200 unlock flow and RLS-gated Supabase calls as
  before — verified by QA against the detail page's pre-existing (previously unreachable)
  decision UI, which needed only the entry link wired, not new logic.
- AC-03: Given a landlord declines then wants to reverse it, then "Undo decline" is
  available on the detail page (did not exist there before this run; added, matching the
  deleted card-view version, with an added affected-rows check the original card version
  lacked).
