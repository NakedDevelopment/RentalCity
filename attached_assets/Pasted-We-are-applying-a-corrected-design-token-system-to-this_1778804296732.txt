We are applying a corrected design token system to this app. The previous developer 
built it from wireframes instead of hi-fidelity Figma designs. We are correcting 
the visual layer only — no logic, routing, API calls, or functionality will be touched.

## PASS 1 ONLY — Do not touch button components yet.

### STEP 1 — Audit first, report before changing anything

Read the codebase and tell me:
1. What CSS framework is in use? (Tailwind version, plain CSS, CSS modules?)
2. Where do CSS variables live? (theme.css, globals.css, index.css, tokens.css?)
3. Is there a tailwind.config.js or tailwind.config.ts?
4. List every file containing hardcoded hex color values — filename and hit count
5. Are shadcn/Radix components present? MUI? Any other component library?

Report this before making any changes. Wait for my confirmation to proceed.

### STEP 2 — Replace CSS variables

Find the primary CSS variables file (:root block) and make these replacements:

OLD → NEW:
- #0EA5E9  →  #3A7AFE  (primary blue)
- #1F2937  →  #171717  (foreground / heading text)
- #6B7280  →  #525252  (muted foreground)
- #D1FAE5  →  #CDF7D4  (accepted badge bg)
- #10B981  →  #0E7B20  (accepted status color)
- #FEF3C7  →  #FAE8C7  (locked badge bg)

Add these new variables to :root if not already present:
--gradient-primary: linear-gradient(83.7338deg, #00BBFF 11.921%, #3A7AFE 90.638%);
--color-primary-light: #00BBFF;
--color-notification: #FF4B4B;
--color-score-excellent: #4CD964;
--color-score-moderate: #FFB020;
--color-status-locked-text: #905F05;
--color-status-accepted-text: #0E7B20;
--color-neutral-900: #171717;
--color-neutral-800: #1C1C1E;
--color-neutral-600: #404040;
--color-neutral-500: #525252;
--color-neutral-400: #737373;
--color-neutral-300: #A3A3A3;
--color-neutral-250: #ADAEBC;
--color-neutral-200: #D4D4D4;
--color-neutral-100: #E5E5E5;
--color-neutral-50:  #F5F5F5;

### STEP 3 — Replace hardcoded hex values across all components

Search every file for these hardcoded values and replace with the CSS variable:

#171717  →  var(--color-neutral-900)
#1C1C1E  →  var(--color-neutral-800)
#404040  →  var(--color-neutral-600)
#525252  →  var(--color-neutral-500)
#737373  →  var(--color-neutral-400)
#A3A3A3  →  var(--color-neutral-300)
#ADAEBC  →  var(--color-neutral-250)
#D4D4D4  →  var(--color-neutral-200)
#E5E5E5  →  var(--color-neutral-100)
#F5F5F5  →  var(--color-neutral-50)
#3A7AFE  →  var(--color-primary)
#00BBFF  →  var(--color-primary-light)
#FF4B4B  →  var(--color-notification)
#4CD964  →  var(--color-score-excellent)
#FFB020  →  var(--color-score-moderate)
#905F05  →  var(--color-status-locked-text)
#0E7B20  →  var(--color-status-accepted-text)
#FAE8C7  →  var(--color-status-locked-bg)
#CDF7D4  →  var(--color-status-accepted-bg)

### STEP 4 — Report when done

Tell me:
1. Total replacements made
2. Every file modified
3. Any values you could NOT safely replace and why
4. Do NOT touch any button components — we will handle those in Pass 2

DO NOT touch: routing, API calls, data fetching, auth logic, form validation, 
or any non-style code.